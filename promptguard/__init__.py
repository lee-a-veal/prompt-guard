"""prompt-guard: indirect prompt-injection detection for Claude Code tool outputs.

Import the scanner explicitly to avoid a circular re-import warning under
`python3 -m promptguard.scan`:

    from promptguard.scan import scan
"""
__version__ = "0.1.0"
